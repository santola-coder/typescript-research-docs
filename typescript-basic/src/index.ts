// 01-basics/variables.tsx

let number: number[] = [20, 30, 40, 50];
let users: string[] = ["San tola", "O tani"];

let sumNumbers = number.map(n =>n+)

// Example 2: Make usernames uppercase
let upperUsers = users.map(name => name.toLowerCase());
console.log(`${upperUsers}`);
